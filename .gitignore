# Dependencies
node_modules/
dist/
dist-ssr/
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Runtime
.env
.env.*
!.env.example

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Database (SQLite)
server/prisma/*.db
server/prisma/*.db-journal
server/prisma/*.db-shm
server/prisma/*.db-wal

# Uploads
server/uploads/*
!server/uploads/.gitkeep

# Build
build/
.cache/
samples/.backup/Pasted image (2).png
samples/.backup/Pasted image (3).png
samples/.backup/Pasted image (4).png
samples/.backup/Pasted image (5).png
samples/.backup/Pasted image (6).png
samples/.backup/Pasted image (7).png
samples/.backup/Pasted image (8).png
samples/.backup/Pasted image.png
